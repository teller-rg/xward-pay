import { ButtonType, PrimaryButton } from '../../../components/PrimaryButton';

export const JoinNow: React.FC = () => (
  <>
    <PrimaryButton title="Join now" type={ButtonType.GRADIENT} />
  </>
);
